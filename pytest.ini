[pytest]
# Add the project root to sys.path so 'client', 'server', 'datasets' etc. are importable
# Works with pytest >= 7.0  (installed version is 9.x)
pythonpath = .
testpaths = tests
addopts = -v --tb=short --cov=. --cov-report=term-missing
